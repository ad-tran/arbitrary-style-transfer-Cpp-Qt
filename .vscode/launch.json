{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug App (Debug Build)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/debug/style_transfer_app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build (Debug)"
        },
        {
            "name": "Debug App (Release Build)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/release/style_transfer_app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build (Release)"
        }
    ]
}
